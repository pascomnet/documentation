pipeline {
    agent {
        dockerfile {
            label 'docker'
        }  
    }
    stages {
        stage('Test') {
            steps {
                sh 'ls -la'
            }
        }
    }
}